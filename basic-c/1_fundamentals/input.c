#include <stdio.h>

int main(void) {

    int num;
    float f;

    scanf("%d", &num);
    scanf("%f", &f);

    printf("The figure read from the keyboard is %d", num);
    printf("\nThis prints a floating point figure: %f", f);


    return 0;
}